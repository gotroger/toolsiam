node_modules/
dist/
.wrangler/
.dev.vars
.env
.astro/
.superpowers/

# macOS
.DS_Store

# ภาพต้นฉบับความละเอียดสูงของภาพปก/โลโก้ — ไฟล์ที่เสิร์ฟจริงถูก generate
# ลง public/ ด้วย `npm run covers` และ `npm run brand` แล้ว จึงไม่เก็บต้นฉบับใน git
# (เก็บสำรองไว้นอก repo — ถ้าไม่มี จะ regenerate ภาพใน public/ ไม่ได้)
assets/
