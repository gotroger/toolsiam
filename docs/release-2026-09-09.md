# เตรียมเผยแพร่ผลแก้ความถูกต้อง — 9 กันยายน 2569

รุ่นก่อนหน้า: `392a61346774e1b5363cba989f45a4aad2642a23` ซึ่ง GitHub Actions run `34277833482` เผยแพร่สำเร็จ

รุ่นนี้รวมการแก้ A01–A17 และทดสอบกรอกจริงตาม [รายงานแก้ไข](tool-corrections-2026-09-09.md); ไม่ใช้คำรับรองว่าข้อมูลถูกต้อง 100% ทุกกรณี เพราะเครื่องมือภาษี สัญญา และวันหยุดมีขอบเขตที่ระบุบนหน้าใช้งาน

ก่อนเผยแพร่ผ่าน unit tests 730 กรณี, typecheck, lint, build, performance budget และเบราว์เซอร์บน Cloudflare runtime จำลองอีก 29 กรณี; ข้อผิดพลาดที่พบและทำซ้ำได้ในขอบเขตการตรวจได้รับการแก้แล้ว

เผยแพร่ผ่าน workflow `CI & Deploy` จาก branch `main` ไป Worker `toolsiam` โดย workflow ตรวจซ้ำก่อน deploy และใช้โดเมน `toolsiam.com` กับ `www.toolsiam.com`

หากพบปัญหาหลังเผยแพร่ ให้ rollback Cloudflare Worker ไปเวอร์ชันก่อนหน้า หรือ revert commit รุ่นนี้แล้วผ่าน CI ใหม่; ไม่ย้อนฐานข้อมูลผู้ใช้เพราะงานนี้ไม่มีการเขียนข้อมูลผู้ใช้

หลักฐานเบราว์เซอร์ของไฟล์ build อยู่ใน `output/playwright/release-2026-09-09/`; หลักฐาน audit และการแก้รอบแรกอยู่ในโฟลเดอร์วันที่เดียวกันแยกไว้โดยไม่เขียนทับ

รอบแรก commit `9f33ac2` เผยแพร่สำเร็จผ่าน [Actions 34310019693](https://github.com/gotroger/toolsiam/actions/runs/34310019693); การตรวจเว็บจริงพบปัญหา A17 จึงแก้และเผยแพร่ต่อ โดยเก็บหลักฐานรอบแรกไว้ใน `output/playwright/production-2026-09-09/`

หลังแก้ A17 ทดสอบกรอกบน Cloudflare preview ผ่าน 29/29 กรณี และจำลองหยุดโหลด JavaScript แล้วพบช่องกรอก 0 ช่องก่อนพร้อม; เมื่อปล่อยให้โหลด การกรอกครั้งแรกคำนวณภาษี 5,050 บาทได้ถูกต้อง
