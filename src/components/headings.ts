/**
 * สเกลหัวข้อระดับ section ที่ใช้ร่วมกันทุกหน้า (แนวเดียวกับ grids.ts)
 *
 * ปกติให้ใช้ `<Section>` ซึ่งใส่คลาสนี้ให้เอง — ค่านี้ export ไว้สำหรับหัวข้อที่
 * ไม่ได้ห่อด้วย section ของตัวเอง เช่น หัวข้อของการ์ดที่มีหัวข้อเดียวอยู่ข้างใน
 */
export const sectionHeading = 'text-lg font-medium tracking-tight text-slate-900 sm:text-xl';
