import type { Draw } from '../types';

/**
 * ผลรางวัลสลากกินแบ่งรัฐบาลรายงวด — แหล่งความจริงเดียวของทั้ง vertical
 *
 * **ยังว่างโดยตั้งใจ** ผลรางวัลเป็นข้อมูลที่คนเอาไปตัดสินใจเรื่องเงินจริง จึงห้ามใส่
 * ตัวเลขที่ไม่ได้คัดจากประกาศของสำนักงานสลากกินแบ่งรัฐบาลเด็ดขาด (ไม่มีข้อมูล
 * ดีกว่าข้อมูลผิด) หน้าที่พึ่งข้อมูลนี้จะ noindex อัตโนมัติจนกว่าจะมีงวดจริง
 * — ดู `LOTTERY_DATA_PAGES` ใน src/lib/noindex.ts และเทสต์ใน draws.test.ts
 *
 * วิธีเพิ่มงวด: docs/data/lottery-draws.md
 *
 * ข้อกำหนดของแต่ละงวด (บังคับด้วย validateDraw ใน draws.ts):
 *   - `date` = วันออกรางวัลจริง YYYY-MM-DD และเป็นส่วนหนึ่งของ URL /lottery/results/<date>
 *   - เลขรางวัลเป็นสตริงเสมอ (เลข 0 นำหน้าสำคัญ) และมีจำนวนหลักตรงประเภทรางวัล
 *   - จำนวนเลขในแต่ละประเภทต้องครบตามประกาศ (รางวัลที่ 2 ห้ามใส่มา 4 เลข)
 *   - `near-first` ไม่ต้องกรอก ระบบคำนวณจากรางวัลที่ 1 (±1) ให้เอง ถ้าไม่ใส่
 *   - `source.verifiedAt` = วันที่คนกรอกเปิดประกาศจริงมาเทียบทีละบรรทัด
 */
export const draws: Draw[] = [];
